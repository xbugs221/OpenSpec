/**
 * Purpose: define OpenSpec-owned path markers without embedding any runtime
 * metadata for external agent integrations.
 */

export const OPENSPEC_DIR_NAME = 'openspec';

export const OPENSPEC_MARKERS = {
  start: '<!-- OPENSPEC:START -->',
  end: '<!-- OPENSPEC:END -->'
};
